# users/models.py

# Optional extension point for custom user model

# Currently using built-in User model (django.contrib.auth.models.User)
# If you later need fields like "department", "role", etc., define them here

# from django.contrib.auth.models import AbstractUser

# class CustomUser(AbstractUser):
#     ...
